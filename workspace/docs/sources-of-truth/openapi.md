# OpenAPI

> **Status:** stub — populated per API as they are architected.

Each API contract is authored as an OpenAPI YAML file and rendered to
browsable documentation with Redoc. CI lints every spec with
`@redocly/cli` before rendering.

## Index

_No APIs have been added yet._

| File | Rendered docs |
|---|---|
| _e.g. `apis/orders.openapi.yaml`_ | _linked once generated_ |
